# The deployment delegates user identity to JDCloud Login and keeps only the Host/Origin fence.
- id: connection
  config:
    browserAuthentication: false
    browserSession: true
    trustedHosts: !!js ctx.webRuntime.trustedHosts
